const fs = require('fs');
const inputData = fs.readFileSync('/dev/stdin').toString().trim().split(' ').map(Number);
const [A, B] = [inputData[0], inputData[1]];

const answer = Math.abs(A-B);
console.log(answer);